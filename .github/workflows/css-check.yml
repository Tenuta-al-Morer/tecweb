name: W3C CSS Validator

on:
  push:
    branches: [ main, master ]
  pull_request:

jobs:
  css-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del codice
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Installa dipendenze validatore
        run: npm install glob w3c-css-validator

      - name: Esegui Validazione CSS (Jigsaw W3C)
        id: css-check
        continue-on-error: true 
        uses: actions/github-script@v6
        with:
          script: |
            // --- CORREZIONE QUI SOTTO ---
            // Rinominiamo la variabile in 'globLib' per non andare in conflitto con GitHub
            const globLib = require('glob');
            const validator = require('w3c-css-validator');
            const fs = require('fs');

            // Usiamo globLib.sync invece di glob.sync
            const files = globLib.sync('**/*.{html,css}', { ignore: 'node_modules/**' });
            
            console.log(`🔍 Trovati ${files.length} file da analizzare...`);
            
            let hasErrors = false;

            const validateFile = async (file) => {
              const content = fs.readFileSync(file, 'utf8');
              try {
                const result = await validator.validateText(content, { medium: 'all' });
                
                if (result.valid) {
                  console.log(`✅ ${file}: OK`);
                } else {
                  console.log(`❌ ${file}: ERRORI TROVATI`);
                  hasErrors = true;
                  result.errors.forEach(err => {
                    console.log(`   -> Riga ${err.line}: ${err.message}`);
                  });
                }
              } catch (e) {
                console.log(`⚠️ Errore di connessione o parsing su ${file}: ${e.message}`);
              }
            };

            for (const file of files) {
              await validateFile(file);
            }

            if (hasErrors) {
              core.setFailed('Trovati errori di validazione CSS (vedi log sopra).');
            } else {
              console.log("🎉 Tutto perfetto! Nessun errore CSS trovato.");
            }